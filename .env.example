# No environment variables are required. context-ops-mcp reads none at runtime —
# it is a local stdio MCP server with no API keys or secrets. This file exists
# only to make that explicit. (The former ANTHROPIC_API_KEY was removed in 2.0.0
# when the LLM-backed revenue-diagnosis tool was dropped.)
