// Fixture: subpath better-auth import. New v2.1 capability — must match.
import { nextJsHandler } from "better-auth/next-js";
import { prismaAdapter } from "better-auth/adapters/prisma";
export { nextJsHandler, prismaAdapter };
